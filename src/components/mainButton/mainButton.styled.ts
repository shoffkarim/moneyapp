import styled from '@emotion/styled'
import { css } from "@emotion/react"

export const MainButtonContainer = styled.div(() => css`
  display: flex;
  align-items: center;
  justify-content: center;
  position: fixed;
  bottom: 30px;
  right: 30px;
`)